---
title: Scrolls Index
kind: template
---

# Scrolls — index (template)

This file is the public template for `scrolls/index.md`. On a fresh fork, copy it:

```bash
cp scrolls/index.example.md scrolls/index.md
```

`scrolls/index.md` is gitignored so your personal index (populated by `bind`) stays local. The bind skill updates it as new concepts, entities, and sources appear.

## Recently added & updated

_(inscribe / bind / divine populate this — capped at ~12 entries, newest first)_

Format: `- YYYY-MM-DD · <kind> · [[page-slug]] — <one-line summary>`

Kind tags:
- `source` — new scroll-source from inscribe
- `entity (promoted)` — new entity page from a multi-source bind promotion
- `concept (new hub)` — new concept page from bind
- `concept (body update)` — substantial new section added during bind
- `entity (body update)` — substantial new section added during bind
- `synthesis` — new synthesis essay from divine

The section is the "what's new" view at a glance. The categorical lists below remain the comprehensive lookup index.

## Concepts

_(bind populates this)_

## Entities

_(bind populates this)_

## Sources

_(bind populates this)_

## Reports

_(bind populates this)_

## Synthesis

_(bind / divine populate this)_
